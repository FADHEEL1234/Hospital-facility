# Hospital Facility

Simple web map project built alongside QGIS data.

## Contents
- `index.html` ? main page
- `style.css` ? styles
- `script.js` ? behavior
- `GeoJson/` ? data layers used by the map
- `HEALTH.qgz` ? QGIS project file

## Run
Open `index.html` in a browser.